version: "2"

run:
  timeout: 5m
  tests: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofumpt

linters:
  default: standard
  exclusions:
    paths:
      - engineering-docs
      - testdata
